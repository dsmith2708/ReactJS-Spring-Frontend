

export const createReducer = (state = { newUser: {} }, action) => {
    switch (action.type) {
        case "Post New User":
            break
        default:
            break;
    }
    return state;
};