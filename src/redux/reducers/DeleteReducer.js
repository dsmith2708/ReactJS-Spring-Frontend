

export const deleteReducer = (state, action) => {
    switch(action.type) {
        case "Select User":
            break;
        case "Delete Selected": 
            break;
    }
    return state;
};