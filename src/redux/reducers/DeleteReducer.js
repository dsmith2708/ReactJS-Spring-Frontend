

export const deleteReducer = (state = {usersToDelete: []}, action) => {
    switch(action.type) {
        case "Select User":
            break;
        case "Delete Selected": 
            break;
    }
    return state;
};