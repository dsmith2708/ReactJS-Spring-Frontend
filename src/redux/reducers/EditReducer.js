

export const editReducer = (state = { editedUser: {} }, action) => {
    switch (action.type) {
        case "Dispatch Edited User":
            break;
        default:
            break;
    }
    return state;
};