

export const viewReducer = (state = {users: [{name: 'dantest'}]}, action) => {
    switch(action.type) {
        case "Get Users":
            break;
        case "Navigate to Edit":
            break;
    }
    return state;
};